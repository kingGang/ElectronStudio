# ===== Go =====
# 编译产物
/bin/
/dist/
*.exe
*.exe~
*.dll
*.so
*.dylib
# 测试与覆盖率
*.test
*.out
coverage.*
# Go 工作区
go.work
go.work.sum

# ===== 前端 (web/) =====
node_modules/
web/dist/
web/.vite/
*.local

# ===== sidecar (二进制/模型/用户配置不入库) =====
/sidecars/**/bin/
/sidecars/**/.venv/
/sidecars/speech/models/
/sidecars/speech/config.json
/sidecars/gesture/*.task
/sidecars/audio/playto
__pycache__/
*.onnx
*.bin
!**/.gitkeep

# 唤醒词(KWS)模型入库：体积 ~12MB，换机器免重下（sherpa-onnx zipformer wenetspeech）。
# 因被 /sidecars/speech/models/ 与 *.onnx 双重忽略，无法用 ! 规则救回（git 不进被忽略的目录），
# 故这几个文件是用 `git add -f` 强制纳入跟踪的：encoder/decoder/joiner.onnx + tokens.txt。
# keywords.txt 由 sidecar 启动时按 config 的唤醒词自动生成，保持忽略（不 add）。

# ===== 运行期数据（用户配置/录制/表情素材，不入库） =====
/bin/
/config.json
/actions.json
/emotions/
# 运行/调试日志与临时音频
*.log
*.err
es.err.log
/sidecars/speech/_tts_test.wav
# 3D 源 CAD（大文件，运行时只用转换后的 .glb；如需重转见 docs）
/web/public/models/*.step

# ===== 编辑器 / 系统 =====
.idea/
.vscode/
.DS_Store
Thumbs.db
/sidecars/audio/musicto

# 调试二进制 / 第三方 app / 运行期素材（不入库）
/electronstudio
/ebtest
/usbscan
/电子脑壳*/
/sidecars/speech/emotions/
/sidecars/video/camcap

# 本地构建产物
electronstudio-cgo
# 官方固件 bin/hex（体积大、非本项目源码，不入库）
ElectronBot-Debugger/
